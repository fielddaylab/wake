#basePath   jobBoard

:: partner.tutorial
@trigger    SceneStart
@when       scene:name == "JobBoard"
@once
@cutscene
{@drKelp}    This is your {map-name}Job Board{/map-name}.
            Select the job on the left. 
            Then accept it on the right!
$set    guide:knowsJobs = true